/** The four top-level modes of the home page, shared so the segmented control,
 * the command palette, and deep-link handling all agree on the same union. */
export type ViewMode = "single" | "bulk" | "transcript" | "watch";
